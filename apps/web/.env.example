# Copy to apps/web/.env

# Leave unset in local dev — Vite proxies /api to http://localhost:3000 (see vite.config.ts).
# Set for deployed environments, e.g. https://api.example.com/api
# VITE_API_URL=
