# NestJS 12 is newer than some first-party companion packages' declared peer
# ranges (@nestjs/config, @nestjs/jwt, @nestjs/throttler). They work fine at
# runtime; this stops npm from failing the install on the peer mismatch.
# Revisit once those packages publish ^12 peer ranges.
legacy-peer-deps=true
